int sumOddLengthSubarrays(int *arr, int arrSize)
{
    int sum;

    for (int i = 0, sum = 0; i < arrSize; i++)
        sum += arr[i];
    for (int i = 0; i < arrSize; i + 2)
    {
        for (int j = )
    }
    return sum;
}